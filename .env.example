# Optional, public (non-secret) configuration.
# Copy to `.env` (or set in Vercel → Settings → Environment Variables) if you want
# "Continue Shopping" to jump to the Catalog microfrontend once the group has
# deployed it. Leave it unset and the app falls back to its own /cart route.
VITE_CATALOG_URL=
