{
  "name": "elan-cart-checkout",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ELAN — Cart & Checkout microfrontend (Vue 3 + Vuetify 3 + Vite)",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/spa-fallback.mjs && npm run build:element",
    "build:element": "vite build --config vite.element.config.js && node scripts/inline-element-css.mjs && node scripts/copy-element.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "pinia": "^3.0.0",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0",
    "vuetify": "^3.8.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "sass": "^1.83.0",
    "vite": "^6.0.0",
    "vite-plugin-vuetify": "^2.1.0"
  }
}
